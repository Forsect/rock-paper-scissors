export enum Hands {
  Rock,
  Paper,
  Scissors,
  None,
}

export enum GameRoomStates {
  Loading,
  Waiting,
  Playing,
  Full,
  Left,
  Error,
}

export enum GameResult {
  Win,
  Lose,
  Draw,
  Playing,
}
